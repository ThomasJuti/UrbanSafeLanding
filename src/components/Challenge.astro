---
---

<section id="reto" class="challenge">
  <div class="container">
    <div class="panel">
      <div class="intro">
        <h2 class="light">El reto de hacer entregas en Bogotá</h2>
        <p class="light-body">
          Hoy el repartidor decide a ciegas. Nadie le dice qué calle evitar a las 9 p.m. ni dónde se concentran los
          hurtos de moto: el <span class="stat-inline">85%</span> de los domiciliarios encuestados en Bogotá ya vivió
          una situación de inseguridad en plena entrega, y <span class="stat-inline">14 de 20</span> localidades no
          tiene un solo reporte público actualizado de hurto. La información vive suelta en grupos de WhatsApp, llega
          tarde y nadie la verifica.
        </p>
        <p class="source-note">Fuente: [completar con la fuente real de la encuesta].</p>
      </div>
    </div>
  </div>
</section>

<style>
  .challenge {
    padding: 20px;
  }

  .panel {
    max-width: var(--max-width);
    margin: 0 auto;
    background: var(--color-ink);
    color: var(--color-bg);
    border-radius: 36px;
    padding: clamp(30px, 6vw, 54px) clamp(22px, 5vw, 46px);
    display: grid;
    gap: 30px;
  }

  .intro {
    display: grid;
    gap: 16px;
    max-width: 62ch;
  }

  h2 {
    font-weight: 800;
    font-size: clamp(28px, 6.6vw, 44px);
    line-height: 1.1;
    letter-spacing: -1.4px;
    margin: 0;
  }

  .light-body {
    margin: 0;
    font-size: clamp(17px, 4.2vw, 21px);
    line-height: 1.7;
    color: #cbd5e1;
  }

  .stat-inline {
    font-family: var(--font-display);
    font-weight: 800;
    font-size: 1.35em;
    letter-spacing: -0.5px;
    color: var(--color-accent-light);
  }

  .source-note {
    margin: 0;
    font-size: 13px;
    color: #64748b;
  }
</style>
